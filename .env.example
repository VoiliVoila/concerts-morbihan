# Clé PUBLIQUE OpenAgenda (lecture seule). Copier ce fichier en `.env` et y
# coller la vraie clé. Le `.env` n'est pas commité (cf. .gitignore).
# En production : définir cette variable dans Cloudflare Pages (Settings → Env).
OPENAGENDA_KEY=
